<div align="center">

# 💌 更新日志

记录“愿此见闻温柔你的日常”的每一次正式发布。

[使用说明](./README.md) · [版本发布](https://github.com/siciyuanweilai/astrbot_plugin_daily_share/releases)

</div>

## 🧰 v1.0.7 · 2026-08-14

> 修复 QQ 空间评论解析、视觉识别超时、设置保存闭环和空内容分类异常，并加固协议解析与数据库关闭过程。

### 🛠️ 现役问题修复

- QQ 空间评论只剥离结构明确的昵称或“回复昵称”前缀，正文中的中文冒号和英文冒号会完整保留。
- QQ 空间配图识别使用一份 60 秒总时间预算，候选模型共享剩余时间，不会因逐个回退而无限延长等待。
- 知识库或推荐库没有有效分类时会记录明确日志并取消本次内容生成，不再触发 `IndexError`。
- 设置页恢复“立即保存”和“重新加载”按钮；设置版本冲突时突出显示重新加载入口，成功重载后自动清除提示状态。

### 🔌 协议与解析加固

- QQ 空间 `gtk` 改由 `skey` 派生，`gtk2` 继续由 `p_skey` 派生，避免两个令牌错误使用同一份盐值。
- H5 响应中的 `ret` 为 `null` 时会正确回落读取 `code`；请求 Header 按名称大小写无关覆盖，避免重复发送 `Referer` 或 `Origin`。
- 百科响应明确返回 `success=false` 时直接拒绝；首页 Feed 缺少结尾 `</ul>` 时仍保留最后一条动态。
- 定时延迟任务移除从未使用的前台执行分支；保留历史模块导出以维持兼容，手动任务拒绝时不再重复关闭同一协程。

## 🧭 v1.0.6 · 2026-08-13

> 完善 daily_life 分享上下文、新闻追问和媒体降级观测链路，减少工具上下文数据库开销，并清理容易造成误判的结构与提示词遗留。

### 🧠 daily_life 分享上下文

- 生活上下文统一调用 daily_life 的公开 `get_share_context(target_umo)` 入口，不再回退旧的 `get_life_context(target_umo)`；未提供新入口的生活插件需要同步更新。
- 完整读取公开契约提供的天气、穿搭、当日基调、当前状态、生理节律、主动分享状态、当前活动、备忘录、关系档案、聊天摘要、地点、近期事件、约定、分享建议和完整时间轴。
- 私聊、群聊和 QQ 空间所能获得的人物相关数据继续由 daily_life 按目标隔离；daily_share 只消费公开结果，不读取生活插件数据库或内部运行时。
- 新闻快照与最近关注序号改为在同一个数据库线程任务、同一条 SQLite 连接中读取，减少每次相关 LLM 工具请求的短连接数量。

### 📰 新闻追问

- `news_link` 的结构化 `index` 参数支持阿拉伯数字、全角数字和中文数字，可识别“第二条”“刚才第十条原文”等单一序号表达。
- 年份、同一句中的多个序号及其他歧义输入会被拒绝，不使用正则命令匹配或关键词列表猜测用户意图。
- 普通 `query` 参数仍按新闻标题和摘要检索；清理“必须转换为阿拉伯数字再次调用”的过时工具提示，避免已经可以直接解析中文序号时额外消耗一轮模型调用。
- 仪表盘中的远程视频统一标记为“视频”，不再显示“外链视频”或“外部链接可能失效”等额外标签。

### 📊 媒体降级台账

- 明确区分整条分享失败与媒体局部降级：文案或其他媒体已经成功送达时，记录保持 `success=1`，并通过 `degraded` 与 `degradation_reason` 保存未完成的媒体步骤。
- 仪表盘新增可点击的“降级”统计筛选，可直接查看视频生成失败、媒体未返回结果或部分发送失败的成功分享记录。
- daily_life 视频任务超时时，降级原因会明确显示“生活插件视频生成超时”，不会只归类为普通接口调用失败。
- 降级记录不会进入整条分享失败的重试队列，避免对已送达的文案或图片重复发送。

### ⚙️ 结构与验证

- `SupportRuntime` 移除 `_SupportComponent1…17` 编号别名，改为直接使用职责明确的服务类名。
- `TaskServices` 的迭代与数量根据 dataclass 字段自动生成；架构测试不再锁死 MRO 长度和固定服务数量，继续验证组合关系、显式依赖和真实服务绑定。
- 补充分享上下文单连接读取、中文序号、降级筛选、视频超时原因和跨插件公开契约回归测试。
- 配合包含视频总超时预算修复的新版 daily_life 使用时，轮询等待和单次状态查询都会受统一截止时间约束，不再在配置超时后继续额外等待。

## 🎨 v1.0.5 · 2026-08-09

> 支持由 daily_share 分别指定 daily_life 的文生图和图生图模型，并精简角色外貌配置入口。

### 🖼️ 分模式生图模型

- 新增 `image_conf.daily_life_text_image_model` 与 `image_conf.daily_life_edit_image_model`，分别匹配 daily_life 的文生图和图生图通道，不再共用单一模型配置。
- daily_life 真正取得角色参考图时使用图生图模型，否则使用文生图模型；模式判断由生活插件在参考图解析完成后执行，避免 daily_share 提前猜测导致选错通道。
- 每种模式都采用模型名称完全一致匹配，不会静默回退到其他模型；同一模型配置多条线路时，仍按 daily_life 中的原有顺序依次容错。
- 原开发阶段使用的 `image_conf.daily_life_image_model` 已移除，不再把一个模型同时用于两种生成模式；升级后按需分别填写两个新字段。

### 🔗 联动与兼容

- 文生图与图生图模型仅通过 daily_life 的公开 `generate_share_image` 入口传递，daily_share 不读取或修改生活插件的内部通道配置。
- 对应模型留空时保持该模式原有通道顺序；两项都留空时不传递新增参数，并继续兼容旧版 daily_life。
- 填写任一模型时需要同步更新 daily_life；旧版生活插件不支持新参数时，会明确提示更新插件，而不是统一显示为普通生图失败。
- 生活插件未安装、未启用、正在重载或生成失败时，原有降级行为保持不变，分享文案仍会继续发送。

## 🛡️ v1.0.4 · 2026-08-07

> 修复仪表盘旧页面覆盖目标独立配置及其他设置的问题，为不同配置区域增加相互独立的并发保存保护。

### 🎯 目标配置

- 确认框架重启只会重新载入已经持久化的配置，不会主动清空目标；此前的“重启后消失”实际是旧页面先覆盖配置、后续重启再载入覆盖结果。
- 修复仪表盘长期未刷新、多标签页并行操作或目标被 `/分享` 指令修改后，旧页面可能覆盖目标独立定时与独立类型序列的问题。
- 群聊、私聊、早报群聊和早报私聊四组目标使用独立配置版本；服务端会在配置事务取得锁后校验，旧版本保存不会进入落盘、回滚或定时任务重建流程。
- 状态轮询期间如果目标仍有未保存修改，会同时保留本地目标内容和原始配置版本，避免错误放行已经过期的数据。
- 目标保存日志增加修改前后的各类目标数量，便于排查配置变化，同时不记录目标账号或会话 ID。

### ⚙️ 普通设置

- 普通设置增加独立配置版本校验；长期停留的设置页、多个浏览器标签页及运行中指令修改配置后，旧页面不再采用最后写入者策略静默覆盖新值。
- 目标版本与普通设置版本相互隔离：目标变化不会阻塞普通设置保存，普通设置变化也不会阻塞目标保存。
- 普通设置接口不再接受四组目标列表，即使通过 `schema_extra` 提交也会忽略；目标列表仅由目标管理接口和 `/分享` 目标指令维护。
- 每次重新进入设置页时，如果没有未保存修改，会重新读取服务端配置；存在本地修改时不会强制刷新。
- 检测到版本冲突时会拒绝保存并提示刷新仪表盘或重新加载设置，磁盘中的当前配置保持不变。

## ⚙️ v1.0.3 · 2026-08-05

> 统一框架配置持久化方式，修复消息处理、随机调度与网络会话中的稳定性问题，并完善测试兼容性。

### 🔐 配置与权限

- 配置保存改为调用 AstrBot 配置对象提供的原生保存接口，不再从插件数据目录推导框架配置目录，也不再自行写入配置文件。
- 同时兼容 AstrBot 的异步与同步配置保存接口，避免插件更新期间因框架接口差异导致配置无法保存。
- 启动时会将既有插件配置文件权限收紧为 `0600`，避免 API 密钥等敏感配置被同机其他用户读取。

### 🧩 消息与调度

- 修复新闻链接回复清理时覆盖整条消息链的问题，仅清理文本组件，图片、语音、回复和 At 等组件会完整保留。
- 随机时段要求结束时间晚于开始时间；填写跨天或无效时段时会输出明确错误，不再静默跳过任务。
- 配置结构与 README 已明确随机时段不支持跨天，智能定时的勿扰时段仍支持跨天。

### 🛠️ 运行稳定性

- 忙碌状态查询改为只读检查，不再为未执行过分享的目标创建并长期保留锁对象。
- QQ 空间 `aiohttp` 与 HTTP/2 会话的关闭、创建和超时重建增加并发保护，避免重复创建未关闭的网络会话。
- 新闻链接事件标记会在处理结束后统一清理，空消息链也不会残留上下文状态。
- 补充配置持久化、文件权限、富消息链、随机时段、目标锁和 QQ 空间会话并发的回归测试。
- 修复 `pytest` 收集与测试模块隔离问题，测试入口同时兼容 `pytest` 和 `unittest`。
- 新增和调整的源码注释、异常信息与运行日志统一使用中文；协议名、接口字段等技术标识保持原样。

### 🔗 生活插件联动

- 生活上下文、联网检索、配图、视频、语音和活动回传统一复用插件级 `DailyLifeBridge`，避免不同服务重复创建桥接实例。
- 媒体生成失败时会区分“生活插件未安装、未启用或正在重载”“未返回有效结果”和“媒体接口调用失败”，进度提示不再统一归因于生成接口。
- 每个异步任务独立保存本次媒体调用状态，共享桥接实例处理并发分享时不会串用其他任务的失败原因。
- 隔离契约测试补齐生活插件的 6 个公开入口，并验证插件热重载后的实例重新解析和媒体入口可用性。
- 测试目录补充包标识和双模式导入，指定任意测试文件顺序时仍兼容 `pytest` 与 `unittest discover`。

## 🛡️ v1.0.2 · 2026-08-01

> 加固仪表盘本地媒体删除边界，修复自动安全检查指出的任意路径访问风险。

### 🔒 文件安全

- 本地媒体仅允许从 `daily_share/Temp`、`daily_life/generated/images` 和 `daily_life/generated/videos` 三个受管目录读取与删除。
- 移除相对路径的当前工作目录回退；绝对路径和相对路径都必须通过受管目录校验。
- 路径会在解析后检查真实落点，位于受管目录内但指向目录外的符号链接会被拒绝。
- 执行文件删除前再次校验路径，避免后续调用绕过解析阶段直接删除非受管文件。
- 目录外路径按“跳过”处理；`daily_share` 请求并由 `daily_life` 生成的配图和视频仍可正常回收。

## 🔧 v1.0.1 · 2026-07-31

> 修复仪表盘删除动态与媒体记录后，本地图片文件未同步回收的问题。

### 🖥️ 动态与媒体

- 从仪表盘删除带本地媒体的分享记录时，会同步回收该记录关联的本地文件。
- 由 `daily_share` 请求、通过 `daily_life` 生成的配图，会随对应分享记录一并回收。
- 多目标早报或手动广播可能让多条分享记录引用同一个本地文件；删除其中一条记录时保留文件，删除最后一条引用记录时再回收。
- 远程 URL、`data:` 和 `base64://` 媒体不属于本地文件，不执行文件删除。
- 删除结果继续分别显示已删除、跳过和失败的本地文件数量，便于确认实际清理结果。

## 🌱 v1.0.0 · 2026-07-29

> 💖 首次正式发布。以“在合适的时刻，自然地分享生活”为核心，完成内容、新闻、媒体、上下文、调度、平台发送、QQ 空间和仪表盘的统一实现。

### ✨ 发布亮点

- 📝 支持问候、新闻、心情、知识、推荐五类日常分享，并可按时段自动选择类型。
- 🧭 支持私聊、群聊、个人微信、QQ 空间及其他具备主动发送能力的平台。
- 🎨 支持文字、配图、视频与语音组合，媒体失败不影响其余内容发送。
- ⏰ 支持固定时间、随机时段、智能定时、高级 Cron、独立目标定时和任务恢复。
- 🖥️ 提供完整仪表盘，用于目标、设置、任务、媒体、历史和 QQ 空间管理。

### 📰 内容与新闻

- 新闻优先使用接口摘要、正文和原文链接，背景不足时通过生活插件公开入口补充联网证据。
- 支持微博、知乎、B站、抖音、快手、头条、百度、腾讯、小红书、网易、新浪、澎湃、第一财经、财联社等热搜源。
- 支持每天 60s 读世界、AI 资讯快报、新闻长图、短链接和原文链接追问。
- 新闻链接只读取成功发送时保存的 JSON 快照，不会在查询时重新抓取远程新闻。
- 快照按发送目标和新闻源保存：默认查询当前目标最新快照，指定来源时查询该来源最新快照。
- 发送失败和后台预抓取不会覆盖已成功发送的新闻快照。
- 知识与推荐支持自定义分类、子话题、类型前缀和主题去重。

### 🧠 上下文与 daily_life 联动

- 分享前可按目标读取平台聊天历史，并结合当前时段、生活状态与群聊热度。
- 分享内容始终融合系统默认人设，不提供独立人设开关或插件内人设 ID。
- 通过 `astrbot_plugin_daily_life` 的公开入口复用生活状态、联网检索、配图、视频、语音和 MemOS。
- 联网检索策略、提供商和失败处理统一由生活插件决定，日常分享插件不保留重复搜索实现。
- 生活插件不可用时自动跳过增强能力，纯文字、新闻、调度和平台发送保持可用。
- 只有成功发送的分享才会回传活动并同步记忆。

### ⏰ 调度与目标

- 支持固定时间、随机时段、智能定时和 5 位、6 位、7 位 Cron 表达式。
- 支持全局类型序列，以及群聊、私聊和 QQ 空间独立类型序列。
- 支持普通分享、早报和 QQ 空间各自独立的定时随机延迟。
- 随机时段和智能定时任务会持久化，重启后恢复未完成任务。
- 仪表盘中可直接填写 QQ 号、群号或会话 ID，并选择负责发送的机器人实例。
- 单机器人实例自动绑定，多实例要求明确选择，内部使用完整会话标识避免串线。
- 支持本地用户称呼映射，并优先读取平台备注、昵称或人格设定中的用户称呼。

### 🎨 多媒体与平台

- 配图、视频和语音默认调用生活插件，任一媒体失败不会阻断其他发送阶段。
- 视频由配图生成，图片与视频不会重复混发。
- QQ 群聊和私聊优先下载远程图片后发送，提高富媒体稳定性。
- 个人微信使用独立图片副本，并按数量自动清理临时文件。
- 支持 QQ / OneBot 私聊、群聊和 QQ 空间，以及个人微信私聊主动发送。
- 其他平台按适配器的主动发送能力工作。

### 🌐 QQ 空间

- 支持发布文字或图片说说，以及查看、详情、点赞、评论和删除自己的说说。
- 支持为 QQ 空间单独选择机器人实例，不改变普通私聊和群聊路由。
- 支持 QQ 空间独立定时、类型序列、智能定时和随机延迟。
- 支持好友动态自动点赞、自动评论和评论楼层自动续评。
- 支持自己说说下的一级评论与多级楼层自动回评。
- 使用评论 ID、回复目标和楼层关系校验回评落点，降低错楼风险。
- 支持正文配图与转发配图识别，可配置视觉模型；识别失败时降级为纯文字评论。
- 自动互动支持处理中、成功和失败状态反馈。

### 🪄 LLM 工具与指令

- 新增 `daily_share`，用于生成并发送文字、新闻长图和多媒体分享。
- 新增 `news_link`，用于查询新闻快照中的链接、摘要、来源和列表。
- 新增 `qzone`，用于查看、发布、点赞、评论和操作 QQ 空间说说。
- 新增 `qzone_auto_interact`，用于手动运行 QQ 空间自动互动。
- 保留 `/分享` 作为确定性触发、广播、配置和排障入口。
- 工具权限区分管理员与普通用户，普通用户仅能操作自己的目标和 QQ 空间。

### 🖥️ 仪表盘

- 提供运行总览、目标管理、手动分享、动态与媒体、历史、日历和失败记录。
- 提供 QQ 空间动态流、发布、点赞、评论、删除和详情操作。
- 设置页展示常用配置，并通过 schema 补齐其他字段。
- 支持多机器人选择、目标独立时间、独立序列和 QQ 空间定时随机延迟。
- 完善加载、空状态、错误状态、键盘焦点、移动端布局和减少动画模式。

### 🧯 架构与稳定性

- 采用明确的服务容器和职责边界，分享、新闻、媒体、发送、调度、快照、数据库和面板各自独立。
- 网络请求使用共享异步会话；数据库、文件和图片处理移出事件循环阻塞路径。
- 发送、模型调用、网络请求和后台任务均具备超时、取消与关闭流程。
- 初始化完成后才注册调度；初始化失败会清理已创建资源，终止时等待后台任务退出。
- 支持并发初始化、重复终止、配置事务、异常回滚和后台任务错误收集。
